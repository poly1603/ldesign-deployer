/**
 * 默认配置常量
 * @module constants/defaults
 */

/**
 * 默认 Node.js 版本
 */
export const DEFAULT_NODE_VERSION = '20';

/**
 * 默认端口号
 */
export const DEFAULT_PORT = 3000;

/**
 * 默认 HTTP 端口
 */
export const DEFAULT_HTTP_PORT = 80;

/**
 * 默认 HTTPS 端口
 */
export const DEFAULT_HTTPS_PORT = 443;

/**
 * 默认工作目录
 */
export const DEFAULT_WORK_DIR = '/app';

/**
 * 默认 Nginx 根目录
 */
export const DEFAULT_NGINX_ROOT = '/usr/share/nginx/html';

/**
 * 默认镜像 tag
 */
export const DEFAULT_IMAGE_TAG = 'latest';

/**
 * 默认镜像仓库
 */
export const DEFAULT_REGISTRY = 'docker.io';

/**
 * 默认 Kubernetes 命名空间
 */
export const DEFAULT_K8S_NAMESPACE = 'default';

/**
 * 默认副本数
 */
export const DEFAULT_REPLICAS = 3;

/**
 * 默认健康检查路径
 */
export const DEFAULT_HEALTH_PATH = '/health';

/**
 * 默认环境
 */
export const DEFAULT_ENVIRONMENT = 'development';

/**
 * 默认部署平台
 */
export const DEFAULT_PLATFORM = 'docker';

/**
 * 默认项目类型
 */
export const DEFAULT_PROJECT_TYPE = 'node';

/**
 * 默认安装命令
 */
export const DEFAULT_INSTALL_COMMAND = 'npm ci';

/**
 * 默认构建命令
 */
export const DEFAULT_BUILD_COMMAND = 'npm run build';

/**
 * 默认启动命令
 */
export const DEFAULT_START_COMMAND = 'npm start';

